server:
  port: 8082

spring:
  application:
    name: mini-weverse-chat
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:5432/mini_weverse_chat
    username: mini_weverse
    password: mini_weverse
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
    open-in-view: false
  sql:
    init:
      mode: always
  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6380}
      database: 1
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:localhost:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        spring.json.add.type.headers: false
        # send()가 Future를 반환하기 전 내부적으로 멈출 수 있는 최대 시간(메타데이터 조회,
        # 버퍼 확보 등). 코드의 Future.get(3, SECONDS)와 맞춰서, 전체 대기가 3초 언저리로
        # 끝나게 한다 — 이 값이 없으면 기본 60초까지 send() 호출 자체가 멈출 수 있다.
        max.block.ms: 3000
    template:
      observation-enabled: true
    consumer:
      group-id: chat-service
      auto-offset-reset: earliest
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      properties:
        spring.json.trusted.packages: com.miniweverse.common.membership
        spring.json.value.default.type: com.miniweverse.common.membership.MembershipSyncEvent
        spring.json.use.type.headers: false
    listener:
      observation-enabled: true

jwt:
  public-key-path: keys/public.pem

community-service:
  base-url: ${COMMUNITY_SERVICE_URL:http://localhost:8081}

internal:
  service-secret: ${INTERNAL_SERVICE_SECRET:mini-weverse-internal-secret}

chat:
  websocket:
    allowed-origins: http://localhost:*

management:
  endpoints:
    web:
      exposure:
        include: health,prometheus
  tracing:
    sampling:
      probability: 1.0
    export:
      zipkin:
        endpoint: http://${ZIPKIN_HOST:zipkin}:9411/api/v2/spans
